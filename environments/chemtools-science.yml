# Optional companion runtime for fixed Chemtools science operations.
# Keep Chemtools MCP itself outside this environment.
name: chemtools-science
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pyscf
  - rdkit
  - openbabel
  - h5py
  - basis_set_exchange=0.12
  - pip
